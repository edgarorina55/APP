Pandas - Panel Data// Python Data Analysis 

.python library for working with data sets
    .analyzing, cleaning, exploring, and manipulating data 
.allows analysis of big data and making conclusions based on statistical theories 

Pandas Series 
.just like a table column
.1-D array holding data of any type 

.if nothing is specified, the values are labeled with their index number
    .can be used to access a specified value 
    .when using key:value pairs, the keys become the labels 

DataFrames 
.are multi-dimensional data tables 
.2-D array holding both columns and rows 

.use the loc attribute to return one or more specified rows
.with the index argument, I can name my own indexes 
    .use the named index in the loc attribute to return the specified row(s)

    pd.read_csv - loads data in csv file into a dataframe 
    
    df.info:    - range index - shows # of entries 
                - # of data columns 
                    .names of each column, with the data type 


Cleaning data 
.fixing bad data in a data set 
    .bad data - empty cells 
                .data in wrong format
                .wrong data
                .duplicates 

    
    .dropna() - returns new dataframe, and does not change the original dataframe 
        .use the inplace = True argument to change the original dataframe 
        

>> working with missing data 
.represented as 
    .none
    .NaN 

    1. using isnull()
    .finding missing values:
        .returns a dataframe of Boolean value where True represents missing data (NaN)

    2. using isna()
    .finding missing values:
        .returns a dataframe of Boolean value where True represents missing data (NaN)

    3.checking for non-missing values using notnull()
    .focusing only on the rows with valid, non-missing values 
        .function returns a dataframe with Boolean values where True indicates non-missing (valid) data 
    
    4. finding the number of missing values in the data set
        > df.isnull().sum() 

    5. using fillna()
    .replacing missing values with a specified value or use interpolation methods to find the missing data 
        .replaces the NaN values with a given value

.info() - tells of how many non-null values are in a column

>> dropping duplicates
    .drop_duplicates()
        .removes duplicate rows from a dataframe based on all columns or specific ones 
            .retains the first occurrence of each row and removes any duplicates that follow 
        .give (keep='last') argument to retain the last occurrence 
        .give (keep=False) argument to remove all rows that are duplicates 
    
    .use inplace=True to modify the dataframe in place without creating a new one 


>> Changing the data type
.correct data type is crucial for accurate computations and analyses 

    .astype()
        .allows conversion of specific columns to desired data types 


    
>Data processing
.its manipulating, transforming, and analyzing data using Python
.contains series of operations changing raw data into structured data/ meaningful insights
.Pandas is a python library that facilitates efficient data processing; built on NumPy
    >processes:
        .loading the data into dataframes
        .printing/displaying the data 
        .summarizing the dataframe 
        .descriptive statistical measures of a dataframe 
        .handling missing data 
        .row & column manipulations 
            .removing rows - df.drop(index)
            .renaming rows - df.rename({key:'value'})
            .adding new columns - df['New Column'] = values to the column 
        .merging dataframes 
            .used for all standard join ops 
            .databases joined based on their common column values 
        .apply function
            .iterates over a dataframe 
            .also used with lambda functions 


>Data Normalization
.its scaling numeric features to a standard range, preventing large values from dominating the learning process 
in machine learning 
.ensures that numerical features (in ML and data analysis) are on a similar scale for optimal model performance 
importance:
    .avoids numerical instability in models
    .speeds up convergence in gradient-based algorithms
    .ensures all features contribute equally to the analysis 

    >steps
        .import the required libraries 
        .load or create a dataset 
        .apply different normalization techniques 
        .visualize the results 

    .Normalization techniques 
    1. Maximum absolute scaling 
    .rescales each feature between -1 and 1 by dividing all values by the maximum absolute value in that column 
    .useful when the data doesn't contain negative numbers, and the need to preserve the data's sparcity

    2. min-max feature scaling
    .also called normalization
    .rescales the feature to a hard and fast range of [0,1] by subtracting the min value of the feature then dividing by the range 

    3.z-score method
    .often called standardization
    .changes the values in each column so that they have a mean of 0 and a standard deviation of 1
    .best when the data follows a normal distribution or when I want to treat values in terms of how far they are from the average



>Data Manipulation
